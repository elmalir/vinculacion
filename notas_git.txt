git clone https://github.com/elmalir/vinculacion.git

or create a new repository on the command line
echo "# vinculacion" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/elmalir/vinculacion.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin https://github.com/elmalir/vinculacion.git
git branch -M main
git push -u origin main
git pull origin master
git pull origin master --rebase
    git rebase --continue
Error fatal: refusing to merge unrelated histories
    git pull origin master --allow-unrelated-histories


BDD pública empresaria1_vinculacionuta
Usuario vinculacion
Contraseña Empdb.V1nc\/l@i0n

FC 002003 2772    id=13333